package com.middleeastcontainer.domain.model

/** Extra-image categories, exact legacy spinner order (first entry is blank). */
object ExtraImageCategory {
    val values: List<String> = listOf(
        "", "Front Side", "Left Side", "Right Side", "Interior", "Floor", "Roof", "Door Side"
    )
}
